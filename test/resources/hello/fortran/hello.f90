! This is a comment
! important to have comments, so similarity test works, otherwise with 1 line fails
! important to have comments, so similarity test works, otherwise with 1 line fails

write(*,"(A)")"* %PRINT% *"
%INCLUDE_EXTERNAL_PRETTY%
